name: CICD Apply

on:
  push:
    branches:
      - dev
  pull_request:
    branches:
      - dev

jobs:
  Apply CICD Process:
    run-on: ubuntu-latest
    steps:
      - name: Need checkout
        uses: action/checkout@v2
