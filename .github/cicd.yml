name: CICD Apply

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  Apply CICD Process:
    run-on: ubuntu-latest
    steps:
      - name: Need checkout
        uses: action/checkout@v2
